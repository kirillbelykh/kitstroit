# KIT Links Hub Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Ship `https://kitstroit.ru/links` — mobile-first KIT-styled link hub with site, Telegram, Max, phone.

**Architecture:** Same SPA path switch as `/privacy` in `main.tsx`. New `Links.tsx` + CSS block in `styles.css`. No new dependencies.

**Tech Stack:** React + existing CSS tokens/fonts; Metrika already inits for non-lab routes.

---

## File map

| File | Role |
|---|---|
| `frontend/src/Links.tsx` | Page markup and link list |
| `frontend/src/styles.css` | `.links-page` styles |
| `frontend/src/main.tsx` | Route `path === '/links'` |
| `docs/superpowers/specs/2026-07-29-links-hub-design.md` | Spec (already written) |

---

### Task 1: Links page component

**Files:**
- Create: `frontend/src/Links.tsx`

**Step 1:** Create component with logo, short line, four links (order: Сайт, Telegram, Max, Позвонить), using `PHONE_DISPLAY` / `PHONE_LINK` from `components.tsx` if present.

**Step 2:** External links: `target="_blank"` + `rel="noopener noreferrer"` for http(s); phone stays `tel:`.

**Step 3:** Commit.

### Task 2: Styles

**Files:**
- Modify: `frontend/src/styles.css`

**Step 1:** Add `.links-page` mobile-first layout: ink background, bone text, brass accents, full-width rows ≥52px, serif brand, no card clutter. Subtle atmosphere (gradient / existing media) allowed.

**Step 2:** Commit.

### Task 3: Wire route

**Files:**
- Modify: `frontend/src/main.tsx`

**Step 1:** `const isLinks = path === '/links'` → render `<Links />` like Privacy.

**Step 2:** `cd frontend && npm run build`

**Step 3:** Commit + push branch `cursor/links-page-2878`.

### Task 4: Report

Return branch, commit hashes, build result, how to open `/links` locally and on prod after deploy.
